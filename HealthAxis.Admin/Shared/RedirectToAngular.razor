@inject NavigationManager Navigation
@inject IConfiguration Configuration

@code {
    protected override void OnInitialized()
    {
        var baseUrl = Configuration["AngularApp:BaseUrl"];

        Navigation.NavigateTo($"{baseUrl}/login", true);
    }
}